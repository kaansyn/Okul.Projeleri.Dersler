using System;

namespace Ders5
{
    internal class Program
    {
        static void Main(string[] args)
        {


            /*  for (int i = 10; i > 0; i--)
              {
                  Console.WriteLine(i);
              }*/

          /*  Console.WriteLine("Başlangıç değerini giriniz...");
            int bas = Convert.ToInt32(Console.ReadLine());
            Console.WriteLine("Bitiş değerini giriniz...");
            int bit = Convert.ToInt32(Console.ReadLine());

            for (int i = bas; i < bit; i+=2)
            {
                Console.WriteLine(i);
            }*/






            int toplam = 0;

            toplam = toplam + 2;
            toplam = toplam + 3;

            Console.WriteLine(toplam);












            //  int a = 5
            //  int b = 10
            //  int sonuc = a+b  (Toplama )
            //  int sonuc = a-b  (Çıkarma)
            // int sonuc = a*b  (Çarpma)
            // int sonuc = a/b  (Bölme)
            // int sonuc = a++ (1 ekleme)
            //int sonuc = a--  (1 çıkarma)
            // int a == b  (eşitlik )
            // >   <  (Küçüktür , büyüktür)
            // <=  >=  (büyükeşit , küçükeşit)
            // a != b  (birbirinden farklı )
            // a%b  (Mod alma(Bölümünden Kalan))
            // &&  (ve)
            // ||  (veya)
            // !: (değil)

        }
    }
}
